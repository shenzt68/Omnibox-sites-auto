name: Update sites.json from MoonTV config

on:
  schedule:
    # 每天 4:10（UTC）跑一次，你可以改时间或加频率
    - cron: "10 4 * * *"
  workflow_dispatch: {}  # 允许手动在 Actions 页面点“Run workflow”

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Run converter
        run: node convert.mjs

      - name: Commit and push if changed
        run: |
          if git diff --quiet; then
            echo "No changes in sites.json, nothing to commit."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add sites.json
          git commit -m "Update sites.json [skip ci]"
          git push
